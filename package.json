{
  "name": "sanju-release-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "compat:staging": "node scripts/check-client-compatibility.mjs",
    "db:migrate:status": "node scripts/apply-sql-migrations.mjs --mode status",
    "db:migrate:baseline": "node scripts/apply-sql-migrations.mjs --mode baseline",
    "db:migrate:apply": "node scripts/apply-sql-migrations.mjs --mode apply"
  },
  "dependencies": {
    "postgres": "3.4.5"
  }
}
